import styled from 'styled-components/native';
import { colors } from './colors';

export const Title = styled.Text`
    color: ${colors.text};
    font-size: 24px;
    font-weight: bold;
    margin-bottom: 24px;
    text-align: center;
`;
